function createContainer() {
    let container = document.createElement("div");
    container.classList.add("container");
    document.body.append(container);
    
    
}

function createGrid() {
    let gridBox = document.createElement("div");
    document.body.div.append(gridBox);
}

createContainer();